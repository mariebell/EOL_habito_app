source 'https://rubygems.org'

gem 'rails', '5.1.6'
gem 'puma', '3.9.1' # A web server
gem 'sass-rails', '5.0.6' # Use Sass for stylesheets
gem 'uglifier', '3.2.0' # Use as compressor for JavaScript assets
gem 'coffee-rails', '4.2.2' # User CoffeScript for .coffee assets and views
gem 'turbolinks', '5.0.1' # Navigates web application faster
gem 'jbuilder', '2.6.4' # Builds JSON APIs with ease

group :development, :test do
  gem 'byebug', '9.0.6', platforms: [:mri, :mingw, :x64_mingw]
  gem 'sqlite3', '1.3.13' # A database for development
end

group :development do
  gem 'web-console', '3.5.1' # Access an IRB console on exception pages or by using <%= console %> anywhere in the code.
  gem 'listen', '3.1.5'
  gem 'spring' # Spring speeds up development by keeping your application running in the background.
  gem 'spring-watcher-listen', '2.0.1'
end

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
